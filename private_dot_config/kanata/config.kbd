;; Caps to escape/control and extra super key configuration
  
(defsrc
  caps
  ralt  ;; AltGr key (right alt)
)

(defalias
  escctrl (tap-hold 100 100 esc lctl)
)

(deflayer base
  @escctrl
  lmet     ;; Map AltGr to Super (left meta/Windows key)
)
